{
  "name": "credit-optimistic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "node": "hardhat node",
    "deploy-dev": "hardhat run --network localhost deploy/deploy.ts",
    "deploy-goerli": "hardhat run --network goerli deploy/deploy.ts",
    "test": "cross-env REPORT_GAS=true hardhat test",
    "build": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.1",
    "hardhat": "^2.12.6"
  }
}
